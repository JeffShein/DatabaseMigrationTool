<Window x:Class="DatabaseMigrationTool.ExportOverwriteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export Already Exists" 
        Height="400" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Warning Icon and Title -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="⚠️" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <TextBlock x:Name="TitleTextBlock" Text="Export Already Exists" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>
        
        <!-- Main message -->
        <TextBlock Grid.Row="1" x:Name="MainMessageTextBlock" Text="An export already exists in the selected directory. The following files will be overwritten:" 
                   Margin="0,0,0,10" TextWrapping="Wrap"/>
        
        <!-- File list -->
        <Border Grid.Row="2" BorderBrush="#CCCCCC" BorderThickness="1" Margin="0,0,0,15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ListBox x:Name="FilesListBox" BorderThickness="0" Background="Transparent">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Margin="5" FontFamily="Consolas"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
        </Border>
        
        <!-- Summary -->
        <TextBlock Grid.Row="3" x:Name="SummaryTextBlock" Margin="0,0,0,15" 
                   FontStyle="Italic" Foreground="#666666" TextWrapping="Wrap"/>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OverwriteButton" Content="Overwrite" 
                    Width="100" Height="30" Margin="0,0,10,0"
                    Click="OverwriteButton_Click"
                    Background="#E74C3C" Foreground="White"/>
            <Button x:Name="CancelButton" Content="Cancel" 
                    Width="100" Height="30" 
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>